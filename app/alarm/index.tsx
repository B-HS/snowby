import { Text } from '@/components/ui/text'
import { ScrollView } from 'react-native'

const Alarm = () => {
    return (
        <ScrollView contentContainerClassName='gap-16 p-16'>
            <Text>Alarm Contents</Text>
        </ScrollView>
    )
}

export default Alarm
